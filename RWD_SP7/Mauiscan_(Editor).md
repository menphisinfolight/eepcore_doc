# Mauiscan (Editor)

> `EEPRWDTools.Core/Editors/Mauiscan.cs` — 21 行
> 繼承：`RWDEditor`

## 用途

**MAUI 掃描輸入編輯器**。專為 .NET MAUI 行動應用提供的掃描輸入元件。

## 設計介面屬性

| 屬性 | 類型 | 設計介面 | 預設值 | 說明 |
|------|------|----------|--------|------|
| **readonly** | bool | `[Security("field")]` | false | 是否唯讀 |

## 備註

- 渲染為 `<input>` 加上 `bootstrap-mauiscan` CSS 類別。
- 與 Scan 類似但針對 MAUI 平台。
